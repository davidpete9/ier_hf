/*
    ier_hf
    ---------------------------

    Jason Project File

    This file defines the initial state of the MAS

    Jason 2.5.1
    April 30, 2022 - 18:04:31
*/

MAS ier_hf {

    
    environment: world.DeliveryEnvironment(3,50,yes)

      agents: 
	  
	  auc auctioneer
	      [mindinspector="gui(cycle,html,history)"]
		  agentArchClass world.AuctioneerArch;
	   
	  
	  drone
	     [mindinspector="gui(cycle,html,history)"]
	      beliefBaseClass agent.DroneParamsBB("weight(10)","speed(20)","range(50)");
	 drone
	      [mindinspector="gui(cycle,html,history)"]
	      beliefBaseClass agent.DroneParamsBB("weight(5)","speed(10)","range(5)");
}          
